version: '3.8'

services:
  whatsapp-http-api:
    image: devlikeapro/whatsapp-http-api
    ports:
      - "3000:3000/tcp"
    stdin_open: true
    tty: true

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: prod
    ports:
      - "80:80"